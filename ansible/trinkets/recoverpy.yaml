# OnlyRoot()
# [ WhichProbe("conda") ]
# Interactively find and recover deleted or 👉 overwritten 👈 files from your terminal 

- name: "Install RecoverPy"
  hosts: localhost
  connection: local
  become: true # Remove this if AnyUserNoEscalation
  gather_facts: false
  tasks:
    - name: "Install via pip"
      register: aptout
      command:
        cmd: pip install recoverpy
    - debug: var=aptout

