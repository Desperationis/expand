# AnyUserNoEscalation()
# [ WhichProbe("conda") ]
# A feature-rich command-line audio/video downloader

- name: "Install yt-dlp"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Install via pip"
      register: aptout
      command:
        cmd: pip install yt-dlp
    - debug: var=aptout

